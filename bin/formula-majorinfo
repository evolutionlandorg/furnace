#!/usr/bin/env bash

set -e

if [ -z "$1" ]
  then
    echo "No argument supplied"
    exit 1
fi

. $(PWD)/bin/init

seth call $FORMULA_PROXY  "getMajorInfo(bytes32)(address,uint16,uint16,uint16)" $1 
