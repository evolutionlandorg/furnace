#!/usr/bin/env bash

set -e

if [ -z "$1" ] || [ -z "$2" ]
  then
    echo "No argument supplied"
    exit 1
fi

. $(PWD)/bin/init

seth call $METADATATELLER_PROXY  "getMetaData(address,uint256)(uint16,uint16,uint16)" $1 $2 
