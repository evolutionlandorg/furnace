#!/usr/bin/env bash

set -e

. $(PWD)/bin/init


tokens=($TOKEN_GOLD $TOKEN_WOOD $TOKEN_WATER $TOKEN_FIRE $TOKEN_SOIL)
for token in "${tokens[@]}"; do
	seth call $APOSTLEITEMBAR_PROXY "enhanceStrengthRateOf(address,uint256)(uint256)" $token $1
done
