#!/usr/bin/env bash

set -e

. $(PWD)/bin/init

path=$FNC_CONFIG
address=$(dapp create src/Formula.sol:Formula)
export FORMULA=$address
seth send $FURNACEPROXYADMIN "upgrade(address,address)" $FORMULA_PROXY $FORMULA
cat $path | jq '.FORMULA = $address' --arg address $FORMULA | sponge $path
