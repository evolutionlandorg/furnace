#!/usr/bin/env bash

set -e

if [ -z "$1" ]
  then
    echo "No argument supplied"
    exit 1
fi

. $(PWD)/bin/init

seth call $ITEMBASE_PROXY  "getEnchantedInfo(uint256)(uint16,bool,address[],uint256[],address[],uint256[])" $1 
