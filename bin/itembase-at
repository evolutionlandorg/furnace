#!/usr/bin/env bash

set -e

if [ -z "$1" ]
  then
    echo "No argument supplied"
    exit 1
fi

. $(PWD)/bin/init

seth call $ITEMBASE_PROXY  "getItem(uint256)(uint256,uint16,uint128,uint256[],uint256[])" $1 
