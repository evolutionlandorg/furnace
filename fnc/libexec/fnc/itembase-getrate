#!/usr/bin/env bash

set -e

. $(PWD)/bin/init

seth call $ITEMBASE_PROXY  "getRate(uint256,uint256)(uint256)" $1 $(seth --to-uint256 $2)
echo "-------------------------"
seth estimate $ITEMBASE_PROXY  "getRate(uint256,uint256)(uint256)" $1 $(seth --to-uint256 $2)
